# tryingAgentSpringboot

A simple Spring Boot 3 demo application.

## Requirements

- Java 17
- Maven 3.6+

## Running the application

```bash
mvn spring-boot:run
```

The application will start on `http://localhost:8080`. A `GET /` endpoint returns `Hello, World!`.

## Running tests

```bash
mvn test
```